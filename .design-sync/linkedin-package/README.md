# LinkedIn Showcase Package

## Ready to upload

- `make-it-count-linkedin-49s.mp4`: 49-second, full 1920x1080 H.264 showcase with an original cinematic electronic soundtrack. Dark mode is the primary presentation, with a brief light-mode reveal.
- `cover.jpg`: opening title image for a video thumbnail.
- `demo-dark.jpg`: dark demo still.
- `demo-light.jpg`: light demo still.
- `workspace.jpg`: signed-in workspace still with private detail removed.
- `features.jpg`: game and challenge feature still.
- `analytics.jpg`: closing analytics/outro still with private labels removed.
- `LINKEDIN-POST.md`: ready-to-paste post copy.

## Publishing notes

Upload the MP4 directly to LinkedIn instead of linking to it. Use `cover.jpg` as the thumbnail if LinkedIn offers a custom thumbnail option. Paste the post text, then keep the demo URL in the body so people can try the read-only experience.

The soundtrack was generated specifically for this package. The signed-in capture uses safe display fixtures for client-loaded lists, and account-specific text is masked before pages render.
